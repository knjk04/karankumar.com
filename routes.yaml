routes:
  /:
    template: home

collections:
  # /:
  #   permalink: /{slug}/
  #   template: index
  /blog/:
    permalink: /blog/{slug}/
    template: index
    # filter: tag:-hash-home

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
